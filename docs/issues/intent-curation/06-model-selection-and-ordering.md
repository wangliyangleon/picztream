# 06 - 模型选择与排序接进 curate

**What to build:** `pzt curate --ai` 的选择不再是随机抽样。模型读着预选集里每张照片的**质量评价与内容描述**，挑出用户要的 N 张，**并决定它们的先后顺序**。

这是整个增量的核心。今天开 AI 时选择是字面意义的 `std::sample`，代码自己写明了原因："没有质量分可比"。现在有描述可读了。

**这次 LLM 调用放在 core**，尽管它不看像素。分层轴已改述为"关于照片的推理归 core，关于用户的推理归 agent"，判据是输入里有没有照片信息（含照片的衍生描述）。取舍与被否掉的替代方案见 ADR-0001。core 现有的 LLM 调用路径都吃图，需要新开一条纯文本路径。

**选择与排序是同一次决定，不拆成两步**（PRD 决策十四）："开头结尾要呼应"这类叙事要求会反过来影响选哪几张，拆成"先选再排"会切在错误的地方。返回顺序即交付顺序。

**模型用序号标识照片**，不用文件路径（PRD 决策十三）：提示词把预选集编成 `1..K`，模型返回序号数组。幻觉面从任意字符串缩成"整数在不在范围内"，约束解码还能直接卡住类型。

**返回值按"清洗后判定"处理**：

1. 剔除越界序号、去重，**保序**
2. 剩余 ≥ N：取前 N，采纳
3. 剩余 < N：**整体退化**成确定性选择

不选"任何不合法就整体退化"（模型返回 9 个好序号外加 1 个越界的，整批扔掉丢的是真信号）；不选"不足时用确定性结果补齐"（补进来的照片既不符合用户偏好、也不在模型排的顺序里，交付的会是两套逻辑拼接的结果，而用户看到的话术只有一种）。

**整批退化的信号必须独立**（PRD 决策二十一）：不能复用既有的"某几个簇比较失败"计数器 - 那个计数器已进用户话术（"哪几组不是 AI 挑的"），混进去会让那句话直接说错。

**测试缝隙**：序号清洗与退化判定做成不依赖网络与数据库的纯函数，表驱动穷举边界。**注意现有 `curate` 的 AI 测试手法是让调用必然失败、只验证退化后的行为，成功路径从未被覆盖** - 本票的校验、排序、分界全在成功路径上，照抄现有手法等于零覆盖，需要注入式的假 HTTP 层（沿用 `core/ai` 各模块既有的 `detail::request_*_impl` 模式）。

**Blocked by:** 03（描述新增 `content` 字段）、04（预选集裁剪与 M 旋钮）、05（curate 内部评估预选集）

**Status:** done（2026-08-01）

- [x] 开 AI 时选择结果不再来自随机抽样
- [x] 模型的输入含每张照片的质量评价**与**内容描述两者
- [x] 模型返回序号而非文件路径
- [x] 返回含越界序号时，越界项被剔除，其余按原序采纳
- [x] 返回含重复序号时，重复项被剔除且保序
- [x] 清洗后有效序号 ≥ N 时取前 N
- [x] 清洗后有效序号 < N 时整体退化成确定性选择
- [x] 整批退化有独立信号，与"某几个簇比较失败"的既有计数器互不干扰
- [x] 交付顺序等于模型返回的顺序，经 headless 输出保真（core 侧有用例；headless 那一段是手工冒烟，见落地记录）
- [x] 序号清洗与退化判定是纯函数，有表驱动边界用例，不需要网络或数据库
- [x] 成功路径有覆盖（注入式假 HTTP），不只测退化
- [x] 关 AI 的行为不受影响

## 落地记录

**纯文本路径做成 `request_json` 的第三个重载，不是新函数**（PRD 待 Eng Design 问题 1 的答案）。三个 provider 的请求体构造对空图片列表天然成立，所以它就是多图重载塞空 vector 的薄壳。唯一要动的是 Local：`images` 为空时**整个省掉这个键**，而不是发空数组 - 那是在向模型声明"这次带了图片，只是零张"。

**选择调用的失败在 curate 那一层折成一个 `nullopt`**。`SelectionError` 分了四种（同 compare/evaluation 的惯例），但对 curate 而言"模型没给出能用的答案"只有一种处置，就是整批退化，具体是哪种失败不改变这个决定。

**`request_selection_impl` 对数组里的非整数元素是跳过、不是整批 `ParseError`。** 规格的清洗列表只点了"越界"与"重复"两项，这是同形推理往上加的第三道过滤：模型返回 9 个好序号外加一个字符串，把整批扔掉丢的还是真信号。真正的整体退化分界仍然只有一处，在 `resolve_selection` 里。

**退化落在与关 AI 完全同一套的确定性路径上**，两条路共用同一个 `deterministic_select`。提成具名 lambda 而不是各写一遍，是为了让"退化后的排列必须与关 AI 一致"这条约束是结构上的，而不是靠约定 - 这跟 PRD 决策十三否掉"用确定性结果补齐"是同一个理由：用户看到的话术只有一种。

**两处本票拍板的边界**（实现前与用户确认）：

1. `ai::request_selection` 的签名**现在就带上 `selection_brief`**（默认空串，为空时整段不进提示词），省得票 08 再动一次签名与提示词。当前恒为空。
2. `ai_selection_fallback` **这次就序列化进 `pzt curate --json`**（仅 `--ai` 时输出，不带 `--ai` 的输出逐字节不变，已冒烟核对）。票 05 落地记录里"未接线的两处"第 2 条只解了一半：`ai_declined`/`cancelled` 仍未序列化，因为那两个钩子在 headless 上还是 nullptr，加进去仍是死字段，留给票 10。

**真机验证结果（2026-08-01）：云端通,本地被 60 秒超时卡死。**

- `--provider gemini`：一次跑通,返回 `{"picks": [5, 3, 6]}`,`ai_selection_fallback=false`,交付顺序与 `picks` 一致。整条链路成立。
- `--provider local`（`gemma4:e2b`，SPEC §3.3 的默认 provider）：请求发得出去,**stderr 上一条 response 都没有**,最后整批退化。`perform_curl_post` 的 `CURLOPT_TIMEOUT` 是 60 秒（`core/ai/ai.cpp`），选择这次调用超了。

**这不是本地模型做不了这件事。** 绕开 60 秒上限、直接对同一个 Ollama 发同样形状的请求（真实长度的中文描述、六条候选、约束解码 + `temperature=0`），返回的是干净的 `{"picks": [1, 3, 5]}`；描述全为空串时也照样吐得出合法序号。是**延迟**不是**能力** - PRD 风险三担心的"本地模型在十几个条目里做可靠取舍的能力"这一条,现有数据不支持,真正的拦路石是那 60 秒。

代码行为本身完全符合规格：调用失败 -> 整批退化 -> 落在与关 AI 同一条确定性路径上,`ai_selection_fallback` 如实为真。同一次运行里还顺带观察到两个信号并存（`ai_fallback_count=1` 且 `ai_selection_fallback=true`），互不干扰,与单测一致。

**留给后续的一个决定（本票不动）**：那 60 秒是 `perform_curl_post` 里所有 AI 调用共用的常量，改它会同时影响评估与锦标赛比较。要么按调用类型分别给上限，要么把它提进 Settings。没有票认领。

**没覆盖到的一处**：`交付顺序...经 headless 输出保真` 里 headless 那一段没有自动化用例。`cmd_curate` 确实按 `result.selected` 的次序输出，但 cli 层没有命令级 JSON 测试的先例（`cli/tests` 只有 i18n/kitty/text/signal 这类纯函数），为这一条现开一个进程级测试缝隙超出本票范围。core 侧的顺序保真有用例钉着，headless 那一段是手工冒烟核对的。
