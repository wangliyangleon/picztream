# 01 - AI 关时交付按拍摄时间排序

**What to build:** 不开 AI 时，`pzt curate` 交出来的照片按拍摄时间排好序，而不是像现在这样在时间上跳来跳去。

今天关 AI 走 farthest-point 贪心选择，最终列表的顺序是那个算法的**挑选顺序** - 而它刻意每次挑离已选集最远的一张，产出的排列在时间上必然跳跃。没有人设计过这个顺序，它只是副产物，而这个列表顺序一路决定了交付时的发送次序（朋友圈九宫格的位置、ins 轮播的滑动次序）。

**选择哪几张的逻辑不变**，只排序。

**Blocked by:** None - can start immediately.

**Status:** done（`158ee3a`，2026-08-01 合入 main）

- [x] 关 AI 时，`pzt curate` 返回的列表按拍摄时间排序
- [x] 选中的是哪几张不因本票改变（同输入下集合与改动前一致）
- [x] 候选集小于 N 时的既有行为不变（本来就已按拍摄时间降序、id 升序）
- [x] 没有 `captured_at` 的照片有确定的落位，不因排序引入不确定性
- [x] 开 AI 的路径不受影响
